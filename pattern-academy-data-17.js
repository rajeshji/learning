const PATTERN_ACADEMY_17 = {
  "lis": {
    title: "Pattern 74 -- Longest Increasing Subsequence (LIS)",
    sections: [
      { type: "callout", heading: "The core idea", tone: "gold",
        body: "This is a MAJOR DP pattern, not just a variation. It introduces the idea that the state is usually 'the best answer ending at this position,' and it has a famous optimization: O(n^2) DP -> O(n log n). Know both for interviews." },
      { type: "diagram", heading: "1. The Problem", dark: true,
        body: "nums = [10,9,2,5,3,7,101,18]. Find the length of the longest strictly increasing subsequence. Answer: 4 -- e.g. [2,3,7,101] or [2,5,7,101]. Remember: subsequence does not need to be contiguous." },
      { type: "chips", heading: "2. Recognition Triggers",
        intro: "Think LIS when you see:",
        items: ["longest increasing subsequence", "longest decreasing subsequence", "maximum number of ordered elements", "preserve index order", "each element must satisfy a relation with the previous selected value", "longest chain", "maximum sequence under <, >, <="],
        note: "Key signal: ONE sequence + SELECT elements + ORDER preserved + RELATION between selected elements." },
      { type: "text", heading: "3. Why Normal DP Isn't Enough",
        body: "[10, 9, 2, 5, 3, 7] -- at 7, we need to know: what is the best increasing subsequence that can be extended by 7? We must look back at previous elements smaller than 7. So the state needs to remember which previous element the subsequence ended at -- that's why dp[i] represents the LIS ending exactly at index i." },
      { type: "callout", heading: "4-5. The Core State & Base Case", tone: "gold",
        body: "dp[i] = length of the longest strictly increasing subsequence whose last element is nums[i]. The phrase 'ending at i' is the most important part.\n\nBase case: every element by itself is an increasing subsequence, so dp[i] = 1 for every i." },
      { type: "callout", heading: "6. The Transition", tone: "red",
        body: "For each i, look at every previous index j < i. If nums[j] < nums[i], we can append nums[i] to the LIS ending at j: dp[i] = max(dp[i], dp[j] + 1).\n\nfor every j < i: if nums[j] < nums[i]: dp[i] = max(dp[i], dp[j] + 1)" },
      { type: "diagram", heading: "7. Java -- O(n^2)", dark: true,
        body: "public int lengthOfLIS(int[] nums) {\n    int n = nums.length;\n    int[] dp = new int[n];\n    Arrays.fill(dp, 1);\n    int answer = 1;\n\n    for (int i = 0; i < n; i++) {\n        for (int j = 0; j < i; j++) {\n            if (nums[j] < nums[i]) {\n                dp[i] = Math.max(dp[i], dp[j] + 1);\n            }\n        }\n        answer = Math.max(answer, dp[i]);\n    }\n\n    return answer;\n}\n\nComplexity: Time = O(n^2), Space = O(n)." },
      { type: "diagram", heading: "8. Walkthrough", dark: true,
        body: "[10, 9, 2, 5, 3, 7] -> dp starts all 1s.\n\n10: nothing before -> dp[0]=1.\n9: 10<9? no -> dp[1]=1.\n2: 10,9 neither smaller -> dp[2]=1.\n5: 2 is smaller (dp[2]=1) -> dp[3]=2, e.g. [2,5].\n3: 2 is smaller -> dp[4]=2, e.g. [2,3].\n7: 2,5,3 all smaller, best is dp[3]=2 (from 5) -> dp[5]=3, e.g. [2,5,7].\n\nFinal answer for this slice: 3." },
      { type: "diagram", heading: "9. The Most Important Mental Model", dark: true,
        body: "For each number:\n\n  current\n     |\nprevious, previous, previous (each with its own LIS length)\n     |\n  choose the best one to extend\n     |\n   dp[i]\n\nThe question is always: what is the best valid sequence I can extend?" },
      { type: "comparison", heading: "10. Why dp[i] Is Different From LCS", items: [
        { label: "LCS", body: "dp[i][j] -- because we compare sequence A against sequence B." },
        { label: "LIS", body: "dp[i] -- because there's only ONE sequence, but we still need to remember the previous selected index through the transition." }
      ]},
      { type: "text", heading: "11. LIS Is a 'Previous Choice' DP",
        body: "Broader pattern: whenever your decision at i depends on which previous element you chose, dp[i] can mean 'best answer ending at i.' This shows up in: LIS, Longest Decreasing Subsequence, Maximum chain, Russian Doll Envelopes, longest arithmetic subsequence variants, and some scheduling/chain problems." },
      { type: "callout", heading: "12. Strictly Increasing vs Non-Decreasing", tone: "red",
        body: "Strictly increasing (1 < 2 < 3, equal values NOT allowed): condition nums[j] < nums[i].\n\nNon-decreasing (1 <= 2 <= 2 <= 3, equal values ARE allowed): condition nums[j] <= nums[i]. Don't confuse them -- this distinction changes both the DP condition and the binary search variant later." },
      { type: "comparison", heading: "13. LIS Is Not Longest Increasing Subarray", items: [
        { label: "Subsequence", body: "Can skip: [2,5,3,7] -> LIS = [2,5,7]." },
        { label: "Subarray", body: "Must be contiguous: [2,5] then 3<5 breaks the run." }
      ], conclusion: "SUBARRAY -> contiguous. SUBSEQUENCE -> can skip. This should be automatic by now." },
      { type: "text", heading: "14. Why Greedy Isn't Obvious",
        body: "[10, 9, 2, 5, 3, 7, 101] -- 'always choose the smallest next value' isn't sufficient on its own. Different subsequences can have the same length but different ending values, and the ending value matters: a smaller ending value gives more opportunities to extend later. This observation leads directly to the O(n log n) optimization." },
      { type: "callout", heading: "15. O(n log n) LIS", tone: "gold",
        body: "Instead of storing dp[i], maintain an array tails[] where tails[k] is the smallest possible ending value of an increasing subsequence of length k+1. This wording is critical." },
      { type: "diagram", heading: "16. Building tails Step By Step", dark: true,
        body: "Process [10,9,2,5,3,7,101,18]:\n\n10 -> tails=[10]\n9  -> tails=[9] (better length-1 ending)\n2  -> tails=[2] (even better)\n5  -> tails=[2,5] (length 2)\n3  -> tails=[2,3] (better length-2 ending than 5)\n7  -> tails=[2,3,7] (length 3)\n101 -> tails=[2,3,7,101] (length 4)\n18  -> tails=[2,3,7,18] (replace 101, length stays 4)\n\nAnswer: 4." },
      { type: "callout", heading: "17. The Crucial Misconception", tone: "red",
        body: "tails is NOT necessarily an actual LIS. tails=[2,3,7,18] may not correspond to one exact subsequence from the original array. Instead, tails stores the best possible ending value for each subsequence length -- that's enough to determine the LENGTH, not the sequence itself." },
      { type: "text", heading: "18. Why Smaller Tail Is Better",
        body: "Two length-3 increasing subsequences: [2,5,10] and [2,5,7]. Which is better for future extension? [2,5,7], because a future value like 8 can extend it but cannot extend [2,5,10]. So for the same length, always prefer the smaller ending value -- the central insight behind tails." },
      { type: "text", heading: "19-20. Where Binary Search Comes In",
        body: "tails is always sorted, e.g. [2,3,7,18]. For each new number, find the first element in tails that is >= the current number (lower_bound), and replace it.\n\nExample: tails=[2,3,7,18], x=5. First tails[pos] >= 5 is 7 (index 2). Replace: tails=[2,3,5,18]. Length stays 4." },
      { type: "diagram", heading: "21. Java O(n log n)", dark: true,
        body: "public int lengthOfLIS(int[] nums) {\n    int[] tails = new int[nums.length];\n    int size = 0;\n\n    for (int x : nums) {\n        int left = 0, right = size;\n        while (left < right) {\n            int mid = left + (right - left) / 2;\n            if (tails[mid] < x) {\n                left = mid + 1;\n            } else {\n                right = mid;\n            }\n        }\n        tails[left] = x;\n        if (left == size) size++;\n    }\n\n    return size;\n}\n\nComplexity: Time = O(n log n), Space = O(n)." },
      { type: "text", heading: "22-23. What Binary Search Finds & When Length Increases",
        body: "For strict LIS, lower_bound(x) means the first position where tails[pos] >= x. tails=[2,3,7,18], x=7 -> first >= 7 is index 2 -> tails[2]=7, no length increase.\n\nLength increases only when x is greater than every element in tails -- binary search lands at pos=size, so size++. Example: tails=[2,3,7], x=10 -> tails=[2,3,7,10], length increases." },
      { type: "text", heading: "24. Why Is tails Sorted?",
        body: "tails[k] is the smallest ending value for length k+1. A sequence of length k+2 must end with a value greater than the ending value of a sequence of length k+1. Therefore tails[0] < tails[1] < tails[2] < ... for strictly increasing LIS -- this sorted property is exactly what enables binary search." },
      { type: "comparison", heading: "25. O(n^2) vs O(n log n)", items: [
        { label: "O(n^2) DP", body: "Easy to derive, easier LIS reconstruction. Know this well." },
        { label: "O(n log n) tails", body: "Harder to derive, needs extra tracking for reconstruction. Both time and space are competitive -- this is the star optimization interviewers look for." }
      ], conclusion: "For a senior/FAANG interview: know both." },
      { type: "callout", heading: "26-27. What Should You Say in an Interview?", tone: "gold",
        body: "Start with: 'dp[i] represents the longest increasing subsequence ending at index i. For every previous j < i, if nums[j] < nums[i], I can extend that subsequence. This gives O(n^2) time.'\n\nThen: 'There is also an O(n log n) solution using a sorted tails array and binary search. tails[k] stores the smallest possible ending value for an increasing subsequence of length k+1.' That demonstrates both correctness and optimization awareness." },
      { type: "diagram", heading: "28-29. LIS Reconstruction With Parent Pointers", dark: true,
        body: "The tails algorithm gives the LENGTH but not automatically the sequence. To reconstruct: maintain tailsIndex[] (index in original array for each length) and parent[].\n\nWhen inserting nums[i] at position pos: parent[i] = tailsIndex[pos - 1]; then tailsIndex[pos] = i.\n\nExample [2,5,3,7]: when adding 7, parent[indexOf7] = indexOf3, parent[indexOf3] = indexOf2. Follow parent pointers backward: 7 -> 3 -> 2, then reverse -> [2,3,7]. Same reconstruction principle as other DP problems (parent pointers on the optimal path)." },
      { type: "variants", heading: "30. Important LIS Variations", items: [
        { id: "1", title: "Longest Decreasing Subsequence", note: "Reverse the comparison: nums[j] > nums[i]." },
        { id: "2", title: "Longest Non-Decreasing Subsequence", note: "Use <= instead of <, and switch the tails binary search from lower_bound to upper_bound." }
      ]},
      { type: "diagram", heading: "31-32. Russian Doll Envelopes", dark: true,
        body: "Envelopes = [width, height]. Find the max number that fit inside each other. Looks like 2D DP, but the classic reduction is: sort by width ascending + height descending, then run LIS on heights.\n\nWhy height descending for equal widths? Two envelopes of the same width can never fit inside each other, so we don't want them both selectable in the LIS together -- sorting height descending for tied widths prevents that. This is a classic 'transform the problem into LIS before applying the algorithm' move." },
      { type: "text", heading: "33. Longest Chain",
        body: "pairs = [[1,2],[2,3],[3,4]] with chaining condition a < b -- another LIS-like problem. Broader pattern: sort/order candidates + find longest compatible chain -> LIS / DP." },
      { type: "text", heading: "34. LIS vs LCS",
        body: "LIS can sometimes be transformed into LCS (original array + sorted unique array -> LCS), but this is usually not the preferred solution -- direct LIS at O(n log n) is much better. Still useful to recognize that many sequence problems can be transformed into one another." },
      { type: "callout", heading: "35. The Deep Pattern: 'Best Ending State'", tone: "gold",
        body: "dp[i] = best answer ending at i. Whenever you see 'choose previous j subject to a compatibility condition,' consider dp[i] = best(dp[j] + contribution) where j < i and compatible(j, i). This is much broader than LIS alone." },
      { type: "diagram", heading: "36-37. Generic Chain DP Template", dark: true,
        body: "for (int i = 0; i < n; i++) {\n    dp[i] = 1;\n    for (int j = 0; j < i; j++) {\n        if (compatible(j, i)) {\n            dp[i] = Math.max(dp[i], dp[j] + 1);\n        }\n    }\n}\n\nThe entire problem becomes: define compatible(j, i). For LIS: nums[j] < nums[i]. For decreasing: nums[j] > nums[i]. For other chain problems, the compatibility condition changes -- this is a major, more general recognition pattern than memorizing 'LIS.'" },
      { type: "variants", heading: "38-42. Five Common Mistakes", items: [
        { id: "1", title: "Confusing dp[i] with 'LIS up to i'", note: "Wrong -- it means LIS ending EXACTLY at i. The distinction matters because the transition needs the last element." },
        { id: "2", title: "Returning dp[n-1]", note: "Wrong -- the LIS may end anywhere. Use answer = max(dp[i]) across all i." },
        { id: "3", title: "Using nums[j] <= nums[i] for strict LIS", note: "Wrong -- strictly increasing requires nums[j] < nums[i]." },
        { id: "4", title: "Thinking tails itself is the LIS", note: "Not necessarily -- it's an auxiliary structure tracking the minimum possible tail for each length. Its SIZE gives the LIS length." },
        { id: "5", title: "Using the wrong binary search", note: "For strict LIS: lower_bound (first >= x). For non-decreasing LIS: upper_bound (first > x). Essential distinction." }
      ]},
      { type: "callout", heading: "43. Interview Mental Model", tone: "gold",
        body: "[10,9,2,5,3,7,101,18] -- don't think 'I remember the LIS code.' Think: one sequence -> choose elements -> order preserved -> each new element depends on previous chosen value -> dp[i]=best chain ending here -> O(n^2) -> can I optimize because states only need the best tail? -> tails + binary search -> O(n log n). That's expert-level pattern recognition." },
      { type: "diagram", heading: "44. Pattern Card", dark: true,
        body: "CORE PATTERN: Longest Increasing Subsequence\n\nTrigger: one sequence + select elements + preserve order + increasing/compatibility condition.\n\nO(n^2) State: dp[i] = LIS ending at i.\nTransition: if nums[j] < nums[i]: dp[i] = max(dp[i], dp[j]+1).\nAnswer: max(dp[i]).\n\nOptimization: tails + binary search.\nTime: O(n log n). Space: O(n)." },
      { type: "checklist", heading: "Tier 7 progress",
        items: [{ label: "68. LCS / Sequence DP", done: true }, { label: "69. Longest Common Substring", done: true },
          { label: "70. Edit Distance", done: true }, { label: "71. Shortest Common Superseq", done: true },
          { label: "72. Distinct Subsequences", done: true }, { label: "73. Interleaving String", done: true },
          { label: "74. LIS", done: true }, { label: "75. Palindrome DP", done: false }] },
      { type: "nextLesson", pattern: "Palindrome DP",
        points: ["The last major pattern in this tier: key mental transformation is String -> two ends -> left + right -> dp[l][r]", "Three related forms: Longest Palindromic Subsequence, Longest Palindromic Substring, Minimum Insertions to Make Palindrome", "Important lesson: 'two ends moving inward' is another powerful way to recognize a DP state"] }
    ]
  },
};
